# ── Python ──────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# ── App runtime / user data (never commit — may contain personal data) ──
launch.log
*.log
config.json
device_registry.json
protocol_library_user.json
protocol_library_auto.json
captures/
Reports/
# The app writes all user data to ~/W1CK3DWizard/ ; nothing above should
# ever appear inside the repo. Listed here as a safety net.

# ── Dev / packaging artifacts ───────────────────────────────
_theme_backup/
dist/
build/
*.zip
*.spec

# ── OS cruft ────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
*.lnk
